# Variables en javascript

## 💡 ¿qué es una variable?
> En algún momento vamos a necesitar almacenar un dato en memoria
> Y para almacenar ese dato en memoria lo vamos a almacenar dentro de una variable (también puede ser una constante)
> No es algo que podamos abrazar, no es algo que podamos tocar

> Para declarar de una variable en JavaScript utilizamos la palabra reservada **let**

> En otros lenguajes, cuando declaramos una variable estamos obligados a definir el tipo de datos.
> En JavaScript esto no es necesario
> Esto no quiere decir que no exista el tipo de datos en JavaScript. Lo que quiere decir es que no es obligatorio definirlo a la hora de crear una variable o una constante

> El valor que almacenamos dentro de una variable puede variar

## 💡 ¿qué es una constante?
> Una constante es básicamente igual que una variable sólo que el valor que almacenamos dentro no se puede sobre-escribir, una vez creado no puede variar

> Para declarar de una constante en JavaScript utilizamos la palabra reservada **const**
